enum AuthStatus {
  unknown,
  authenticated,
  unauthenticated,
}

enum SignInStatus {
  initial,
  submitting,
  success,
  error,
}

enum SignUpStatus {
  initial,
  submitting,
  success,
  error,
}

enum NavigationTab {
  homeTab,
  reservationTap,
  accountTap,
  settingsTab,
}

enum AdminNavigationTab {
  addMenu,
  deleteMenu,
  customerOrders,
  salesReports,
}
